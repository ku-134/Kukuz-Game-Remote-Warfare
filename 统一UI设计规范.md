# 统一UI设计规范（Remote Warfare）

> 界面与视觉的最高约束。与「统一开发规范.md」同属必读文件。

## 0. 视觉基调

绿色磷光屏。底色黑，绿色文字/线条。

| 色 | 值 |
|---|---|
| 亮绿 | #33FF33 / #00FF00 |
| 中绿 | #00CC00 / #00AA00 |
| 暗绿 | #003300 |
| 黑底 | #0A0F0A / #001100 |

整体氛围：旧电脑终端 + 军用战术网格 + 真实战争元叙事。

## 1. 显示层与交互层分离

- **交互层**：按钮、滑条、可点区域。统一使用交互态（默认 / 选中 / 点击）。
- **显示层**：信息展示、弹窗、介绍文案。统一使用展示态，不承担点击职责。
- 两个层风格兼容但视觉可区分（如交互层有选中反色，显示层无）。

## 2. 按钮规范

- 统一按钮元素：默认绿框 + 暗绿填充；选中（悬停/聚焦）反色高亮；点击有按压反馈。
- 尺寸、字距、边距统一，禁止一个界面大按钮另一个界面小按钮。

## 3. 弹窗规范

- 点击交互层后弹出的窗口属于「显示层」。
- 依据文本内容决定形态：说明/介绍类 → 居中弹窗；大段详细阅读 → 全屏覆盖层。
- 弹窗必须有明确的关闭/返回入口。

## 4. 界面统一性

- 界面分区固定：返回按钮、确定按钮位置全局一致，禁止此界面居中、彼界面跑左上角。
- 每个界面只做一件事：要么交互，要么展示，不混。

## 5. 文本描述与显示层关联

- 文本与显示层强关联：
  - 强阅读区（详情/介绍）→ 文本可偏详细。
  - 介绍文案区 → 只介绍背景/设定，不讲解机制。
  - 机制说明区 → 只讲机制与数值。
- 显示层必须可被识别：本规范要求「介绍层不介绍机制、机制层不渲染背景」。

## 6. 图标与表情

- **一律禁用 Emoji**。
- 图标一律 SVG（内联或独立文件），保持单色绿色系，随显示设置可调。

## 7. 显示效果设置

- 用户可调节：磷光屏强度、扫描线密度、辉光、屏幕弯曲/畸变、亮度。
- 调节过程不影响游戏运行、展示与交互。
- 「打单块」小游戏作为显示设置中的效果展示与适应彩蛋（遵守本规范）。
