# Remote Warfare · 项目开发与维护规则

> 本文件为长期约束文件。每次任务开始前须阅读；若与临时要求冲突，以「统一开发规范.md」为最高优先级，并先向大贤者指出冲突。

## 一、项目信息

- GitHub 仓库：https://github.com/ku-134/Kukuz-Game-Remote-Warfare
- 本地根目录：/storage/emulated/0/个人网站/开发中/Remote Warfare/
- 线上试玩地址：https://ku-134.github.io/Kukuz-Game-Remote-Warfare/
- 技术栈：纯静态 HTML + CSS + JavaScript（零构建、零依赖），Canvas 渲染
- 统一规范文件：统一开发规范.md
- 版本文件：VERSION
- 主分支：main
- 本地备份根目录：/storage/emulated/0/个人网站/开发中/Remote Warfare/备份/
- 部署方式：GitHub Pages（push main 自动发布）

## 二、最高优先级规范

1. 统一开发规范.md 是最高优先级规则文件，包含描述、必须要求、代码风格、目录结构、命名规范、性能要求、兼容性要求、测试要求和发布规则。
2. 每次任务开始前，必须先阅读统一开发规范.md。
3. 如果统一开发规范.md 与临时要求冲突，以统一开发规范.md 为准，并先向大贤者指出冲突。
4. 如果需求不明确，先问大贤者，不要直接猜。

## 三、每次任务开始前

1. 阅读统一开发规范.md、README.md、CHANGELOG.md、版本文件（VERSION）和相关源码。
2. 用简短清单复述：本次需求、影响范围、计划修改的文件、测试点、预计版本号变化。
3. 每次新会话开始时，先确认已阅读统一开发规范.md，并告诉大贤者当前版本号、当前分支、最近一次大版本备份路径。

## 四、开发过程中

1. 保持现有架构和风格，小步修改，不破坏已有功能。
2. 新增功能优先模块化。
3. 资源路径必须兼容 GitHub Pages 的 base path（使用相对路径）。
4. 注意网页小游戏特有事项：移动端适配、键盘/触屏操作、音效自动播放限制、控制台无报错、首屏加载性能、缓存问题。
5. 修改后必须做基本测试：页面能打开、核心玩法可走通、开始/重开/胜负逻辑正常、控制台无明显错误。

## 五、每次完成更新后必须自动执行

1. 自动更新 README.md，包括：功能列表、操作说明、在线试玩链接、运行/构建/部署方式、目录结构、版本号、截图或 GIF（如有变化）。
2. 同步更新 CHANGELOG.md，按 Added / Changed / Fixed / Removed 分类。
3. 更新版本号，遵循语义化版本：
   - PATCH：修复 bug，如 1.0.1
   - MINOR：兼容性新功能，如 1.1.0
   - MAJOR：大版本或不兼容更新，如 2.0.0
4. 每次完成代码更新后，README.md 和 CHANGELOG.md 不允许遗漏。

## 六、大版本备份规则

1. 当版本号推进到新的大版本，即 MAJOR 位增加时，例如 1.x.x -> 2.0.0，必须自动在本地备份根目录下备份一份完整项目。
2. 备份命名：备份/Remote Warfare_v<新版本号>_<YYYYMMDD-HHMM>/
3. 备份内容：源码、资源、配置、README、CHANGELOG、版本文件。
4. 排除：node_modules/、.git/、可重新生成的 dist/，除非大贤者特别要求。
5. 备份后校验文件数量和大小，并在汇报中给出备份路径。
6. 如果当前环境无法直接操作本地文件，必须生成备份脚本，例如 scripts/backup.sh 或 scripts/backup.ps1，并明确告诉大贤者需要执行哪些命令，不要假装已经备份。

## 七、Git 规则

1. 提交信息使用：feat: ...、fix: ...、docs: ...、chore: ...
2. 每个独立功能或修复完成后提交，不要把所有改动堆在一起。
3. 不提交密钥、大文件、node_modules/。
4. 推送前先拉取，冲突先解决，不要强推主分支。
5. README.md、CHANGELOG.md、版本号更新应与代码改动一起提交。

## 八、每次完成后的汇报格式

- 变更摘要：
- 修改文件：
- 测试结果：
- 版本号变化：
- README 是否更新：
- CHANGELOG 是否更新：
- 是否触发大版本备份：是/否，备份路径：
- Git 提交/推送状态：
- 风险与下一步：

## 九、异常处理

1. 如果无法访问 GitHub、本地文件或网络，先明确说明，不要编造执行结果。
2. 任何删除、覆盖、强推、重置等破坏性操作，必须先问大贤者。
3. 若统一开发规范.md 与大贤者的临时要求冲突，以统一开发规范.md 为最高优先级，并先向大贤者指出冲突。

---

以上规则请长期遵守。若无法直接操作本地或 GitHub，须输出补丁、命令和文件内容，并明确标注哪些需要手动执行。
