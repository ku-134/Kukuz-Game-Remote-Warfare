# 统一开发规范（Remote Warfare）

> 最高优先级规则文件。每次任务开始前必读；与临时要求冲突时以本文件为准，并先向大贤者指出冲突。

## 0. 项目描述

《Remote Warfare · 远程战争》是一款「战棋指挥」元素的网页小游戏，强调面对失控的战局进行指挥。核心流程：

指挥单位（操作）→ 结算（行动判定）→ 单位完成指令（返回结果）

采用线性闯关模式，前几关为教程，逐步引入机制。玩法融入海量变量（地形、单位、Buff 等），因此本项目格外强调「规范的项目结构」以支撑长期维护。

## 1. 必须要求

- 纯静态、零构建、零依赖，HTML + CSS + JavaScript，Canvas 2D 渲染。
- 强制横屏；竖屏时显示横屏提示。
- 画面风格：绿色磷光屏（军用网格 + 等高线 + 旧电脑界面 + 真实战争元叙事）。
- 全局命名空间 `RW`，多文件按序 `<script>` 加载，兼容 file:// 与 GitHub Pages。
- 禁用 Emoji 表情；图标一律使用 SVG。
- 显示效果必须可由用户调节（磷光屏强度、扫描线等），且不影响运行/交互/展示。

## 2. 代码风格

- 命名：模块/函数用 camelCase，常量 UPPER_SNAKE，类 PascalCase。
- 每个文件顶部标注模块职责（一段注释）。
- 函数短小单一，避免 God 函数；注释解释「为什么」而非「是什么」。
- 所有模块挂在 `RW` 命名空间下，不污染全局。
- 状态变更集中在明确的状态机（见 `core/engine.js`）。

## 3. 目录结构

```
Remote Warfare/
├── index.html
├── css/style.css
├── js/
│   ├── main.js            # 入口
│   ├── core/              # 引擎核心
│   │   ├── utils.js       # 工具
│   │   ├── engine.js      # 循环/状态机/渲染调度
│   │   ├── input.js       # 键盘+触屏统一
│   │   └── settings.js    # 显示设置
│   ├── systems/           # 玩法系统
│   │   ├── terrain.js     # 地形（等高线/河流/高度场）
│   │   ├── unit.js        # 单位（人数/物资/Buff）
│   │   ├── combat.js      # 结算/行动判定（WEGO）
│   │   └── level.js       # 关卡
│   ├── ui/
│   │   └── ui.js          # UI 框架（按钮/弹窗/显示层）
│   └── data/              # 静态数据
│       ├── unitTypes.js
│       └── levels.js
├── 统一开发规范.md
├── 统一UI设计规范.md
├── README.md
├── CHANGELOG.md
└── VERSION
```

## 4. 命名规范

- 项目仓库：`Kukuz-Game-Remote-Warfare`，遵循 Kukuz 游戏仓库规范。
- 版本文件：`VERSION`，纯文本，内容为语义化版本号。

## 5. 性能要求

- 使用 requestAnimationFrame；逻辑步长固定（dt 上限截断）。
- 避免每帧操作 DOM；UI 仅在状态变化时更新。
- 首屏轻量，控制台无报错、无未捕获异常。

## 6. 兼容性要求

- 移动端横屏 + 触屏，桌面键盘可操作。
- 音效遵循浏览器自动播放限制（用户手势后播放）。
- 资源路径全部使用相对路径，兼容 Pages base path。

## 7. 测试要求

- 每次修改后：页面能打开、核心流程可走通、开始/结算/胜负逻辑正常、控制台无错误。

## 8. 发布规则

- 语义化版本：PATCH 修 bug / MINOR 兼容新功能 / MAJOR 大版本或破坏性更新。
- 每次更新同步 README.md 与 CHANGELOG.md，并与代码一起提交。
- MAJOR 递增时本地备份到 `备份/Remote Warfare_v<版本>_<YYYYMMDD-HHMM>/`。
